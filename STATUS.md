# Status

One row per work package in [ROADMAP.md](ROADMAP.md). States come from the project's verify scripts and the files named as evidence, never from optimism: `done-verified` (acceptance criteria pass, evidenced), `partial` (some but not all acceptance criteria pass), `open` (not yet proven, regardless of any design/draft docs that may exist). Update this table in the same commit that satisfies a task's acceptance criteria ([CLAUDE.md](CLAUDE.md)).

**Milestone M1 (sim)** — [ROADMAP.md](ROADMAP.md)'s own definition, "a simulated UE attaches to Open5GS and uses NOMAD + Matrix with all backhauls down" — is reached as of 1.3-f: `./island.sh up` on a bare checkout (WAN disconnected after image pulls) brings up the real stack end-to-end, and `stack/services/verify.sh` drives the UERANSIM simulator through exactly that scenario. See [QUICKSTART.md](QUICKSTART.md).

**Milestone M3 (sim)** — [ROADMAP.md](ROADMAP.md)'s definition, "published threat model; a SIM from island A roams onto island B in the lab" — is reached as of 3.3-c v2h (2026-09-17): `docs/threat-model.md` is published (v0.1, external review still pending under 5.1), and `stack/federation/two-island.sh --log-level debug roam --ue oai` proves, on one host, a subscriber of island A (PLMN 001/01) registering on island B (PLMN 999/70) through B's own OAI gNB: authentication served by A's AUSF/UDM over SEPP/N32 (K never leaves A), the PDU session anchored at B's SMF/UPF (Local Breakout, address from B's UE pool), and a breakout ping to B's services subnet at 0% loss — all acceptance lines YES on two consecutive runs by the executor and one independent re-run by the review, island A's own core untouched throughout. Roaming stays software-only until a partner lab repeats it over the air ([docs/partner-labs.md](docs/partner-labs.md)); 3.3-d (4G) and the roaming policy-record transport (3.4) remain open. The 4.1 partition drill is now done (see below).

| WBS | Work package | State | Evidence | Date |
|---|---|---|---|---|
| 0.1 | Architecture RFC | done-verified | `rfcs/rfc-0001-architecture.md` | 2026-09-10 |
| 0.2 | Licensing & upstream policy | done-verified | `CONTRIBUTING.md`, `LICENSE` | 2026-09-10 |
| 0.3 | Org scaffolding (repo, CI, Matrix space, templates) | partial | `README.md`, `STATUS.md`, `QUICKSTART.md`, `.github/`. 0.3-d CI (`.github/workflows/ci.yml`): on every push a plain Linux x86 `ubuntu-latest` runner (4 vCPU, 16 GB, unemulated -- everything before this was one Apple-silicon host under amd64 emulation) runs `checks` (82 + 77 pytest, the golden rule, `bash -n`, `docker compose config` on all 11 compose files, shellcheck errors-only, no-`latest` image guard) and `core-sim` (`stack/core/verify.sh` end to end: `ALL CHECKS PASSED` in 30 s, job 1 min 06 s with the image cache warm; `/dev/net/tun` + `NET_ADMIN` confirmed permitted). Green run: <(the workflow's run history: https://github.com/kerbgr/resccom/actions/workflows/ci.yml)>. Guard proven to bite: a throwaway hand-edit of a rendered file turned `checks` red (<(the workflow's run history: https://github.com/kerbgr/resccom/actions/workflows/ci.yml)>), then reverted. `federation-sim` (`two-island.sh verify` + `roam --ue oai`, the M3 vehicle) is manual + nightly only, not required: one green dispatch <(the workflow's run history: https://github.com/kerbgr/resccom/actions/workflows/ci.yml)> (verify 129 s, roam 136 s, all acceptance lines YES, peak host memory 3.3 GB). **CI deliberately does not cover:** anything radiating RF (`[HW]`), Starlink/WAN measurements, the two-island path on ordinary pushes. Two Linux-portability bugs were found by the first runs and fixed: GitHub's default `run` shell has no pipefail (a `\| tee` masked a harness failure -- now `shell: bash`), and `two-island.sh` only parsed macOS pipx's polyglot wrapper for island-init's interpreter. Still open in 0.3: Matrix space. | 2026-09-17 |
| 0.4 | Regulatory/spectrum matrix | partial | `docs/spectrum/README.md` | 2026-09-10 |
| 1.1 | Core bring-up (Open5GS, compose, UERANSIM verify) | done-verified | `stack/core/verify.sh` | 2026-09-11 |
| 1.2 | RAN bring-up (srsRAN ZMQ → SDR, SIM programming) | partial | `stack/ran/verify-4g.sh` (4G: passes); `stack/ran/verify-5g.sh` (5G/srsRAN: negative finding — `gradiant/srsran-5g`'s ZMQ entrypoint corrupts its own config, every tag since 2024; no compatible open-source ZMQ 5G SA UE exists among approved upstreams either); `stack/ran/verify-5g-oai.sh` (5G/OAI follow-up `1.2-a(5G-alt)`: **passes** — OAI gNB+nrUE over rfsimulator register, get a UE IP, and pass local breakout against the same real Open5GS 5GC, with the primary UERANSIM path re-verified with no regression; run twice clean, both `ALL CHECKS PASSED`). 1.2-a's 5G half is therefore met via OAI; 1.2-b/c/d remain open, `[HW]`. Full trace: `stack/ran/README.md` "5G ZMQ rig — negative finding" and "5G-alt rig: OpenAirInterface over rfsimulator — positive finding" | 2026-09-13 |
| 1.3 | Services (NOMAD, Matrix, Jitsi, DNS/portal) | done-verified | `stack/services/verify.sh`, `island.sh` | 2026-09-12 |
| 1.4 | Backhaul module (Starlink, failover, QoS) | partial | `stack/backhaul/verify.sh`, `unplug-test.sh`, `qos-test.sh` | 2026-09-12 |
| 1.5 | PoC demo & write-up | partial | `docs/poc-writeup.md`, `docs/demo-storyboard.md` (write-up done; demo video not yet recorded) | 2026-09-12 |
| 2.1 | Node OS image (unattended install, signed) | partial | `island-init/island_init` (2.1-a: schema v0 + `island-init check` + `island.example.yaml`; 2.1-b: `island-init render` + Jinja templates in `island_init/templates/`). `pytest` 29 passed, including the golden-rule check that `render --lab` reproduces every committed `stack/` config byte-for-byte apart from its own "do not hand-edit" header lines. That render was applied and committed: `stack/core/config/{amf,mme,nrf,smf,upf}.yaml`, `stack/core/config/freeDiameter/{hss,mme,pcrf,smf}.conf`, `stack/services/config/coredns/island.zone`, `stack/backhaul/config/uplinks.yaml` now carry the header (re-running `render --lab --diff` reports "no changes"); `stack/services/config/portal/settings.json` + `island-init/lis-geometry.json` are new, generated files; `stack/services/config/portal/server.py` now reads `settings.json` (association name/operator contact) with a fallback to the exact pre-2.1-b placeholder text when null, verified byte-identical to the old hardcoded output for the lab profile. Full stack cycled (`island.sh down` + `up`) and re-verified clean on the rendered configs: `stack/core/verify.sh`, `stack/services/verify.sh` (all 8 checks incl. portal), `stack/backhaul/verify.sh` — `ALL CHECKS PASSED` on each. 2.1-c: `island-init new` (wizard) added — `island_init/crypto.py` (Ed25519 document signing, X25519 WireGuard keypairs) + `island_init/wizard.py`; `new --lab` reproduces `island.example.yaml` exactly apart from freshly generated keys (verified: every other field byte-equal); `new --allocation <file>` drives the interactive name/country/node-class/profile prompts against a pasted-in registry-allocation file (no `registry/` repo exists yet, WBS 0.3 follow-up — this is the interim input format). `check` now also verifies a present `signature` block and requires one for `profile: production`; tampering one byte of a signed file (a real end-to-end test: sign in memory, write YAML, reload, mutate, reload again) makes `check` fail, both via unit test and the CLI directly. `pytest` 53 passed. Measured end-to-end (`island-init new --lab` → `render` → `./island.sh up` → `stack/services/verify.sh`, images pre-pulled): 2m 35s total, well inside RFC-0005 D4's 30-minute target — recorded in island-init/README.md "Measured timing" (this covers the `--lab` path only; a real volunteer-with-registry-allocation rehearsal needs the `registry/` repo, still open). 2.1-d: Island Console (`console.island`, `stack/services/config/console/` — see `stack/services/README.md` "Island Console" for the full design) added. Map tiles: `maps.island` (`config/nomad-gateway/nginx.conf`) serves the real PMTiles file NOMAD's own Maps feature produced (`setup-world-basemap`, a genuine live call to `build.protomaps.com`, ~14 MB, z0-5) directly off NOMAD's bind-mounted storage — no second tile stack. MapLibre GL JS 4.7.1 + PMTiles JS 4.5.0 vendored (BSD-3-Clause, `config/console/vendor/`), style derived from NOMAD's own downloaded Protomaps style with label layers stripped (no glyph/sprite server vendored). Backend imports `island_init` directly (schema/render/signing) so console and CLI can't drift; Apply re-signs with the association's private key, renders, and restarts only the containers whose config actually changed via the host's Docker socket, logging every apply with a timestamp. Predicted coverage: a named, documented Okumura-Hata model computed from a cell's real `tx_power_dbm` (null for every lab RAN rig — no predicted layer renders for them, no invented numbers); measured: operator-uploaded CSV, hatched-vs-plain-point styling kept visually distinct with a permanent legend, matching RFC-0006 D2. Verified live end-to-end with a real headless browser (`stack/services/verify-console.js`, Playwright, run against `--network container:core-ue-1` — the same UE-subnet pattern `verify-jitsi-call.js` uses, WAN-independent by construction since every asset is vendored): loads `console.island`, confirms the map rendered real features from local tiles, places a site + cell, runs Preview, Applies — `island.yaml` on disk then passes `island-init check` (signature verified), and `render --diff` from the console's `/api/preview` was directly confirmed byte-identical to the CLI's `render --diff` output on the same document. A path-traversal issue in the measured-CSV upload's site-id handling was found and fixed during this same verification pass. SECURITY.md's lab-profile register has this row (no auth, docker.sock + repo write + private key access — same shape of privilege NOMAD's own admin container already has). 2.1-f: console hardening + fresh-clone bring-up. `services-console-1` now mounts `island.yaml` and `/repo` read-only, has no `secrets/` mount, and no `docker.sock` (confirmed via `docker inspect`); it only edits/validates a draft (`POST /api/draft`) written to `data/console/draft.yaml`. Signing, rendering, writing `island.yaml`, and restarting affected services moved host-side into a new `island_init.apply` module / `island-init apply` CLI command, invoked by a new `island.sh apply` subcommand — `pytest` +5 (58 passed total in island-init). Every console write endpoint (`/api/preview`, `/api/draft`, `/api/measured/*`) now requires a per-boot operator token `island.sh up` generates and prints once (`hmac.compare_digest`); confirmed live from inside the UE network namespace: 401 with no/wrong token, 200 with the right one. `island.sh up` now also bootstraps `island-init/island.yaml` (`island-init new --lab`) and `secrets/` before `stack/services` comes up, with an explicit guard-and-fail-loudly check for the directory-instead-of-file symptom a missing `island.yaml` used to cause. **Literally verified**: deleted all ResCCOM-related Docker containers/volumes/networks on the test host, did a real `git clone` into an empty directory, `pipx install ./sim-tools ./island-init`, `./island.sh up` (1m 58s) → `stack/services/verify.sh` (43s, `ALL CHECKS PASSED`) — total ~2m 41s, no undocumented step. Found and fixed one real bug this way: a bash `local a=... b="$a/..."` unbound-variable crash in `bootstrap_console_token` (single-statement cross-reference isn't visible until word-expansion completes — split into two statements). `verify-console.js` updated to the draft flow (401/wrong-token checks, Preview + Save Draft with the real token, confirms a pending draft) and run live end-to-end against the fresh clone: `ALL CHECKS PASSED`; the subsequent `island.sh apply` on the host signed the staged draft and `island-init check` passed. Note found but out of 2.1-f's scope: the committed `island-init/lis-geometry.json` has drifted from `island.example.yaml` (an extra site, likely left over from an earlier live console verification that was applied against the tracked lab files) — `island-init`'s own golden-rule pytest catches this; not touched here since it predates and is unrelated to the console-hardening/fresh-clone work. SECURITY.md's console row updated to the reduced surface. 2.1-g: generated per-island artifacts moved out of tracked paths. `stack/services/config/portal/settings.json` and `island-init/lis-geometry.json` (both per-deployment outputs of whatever `island.yaml` render is pointed at — a real association's operator contact / site geometry) now render to gitignored `stack/services/data/portal/settings.json` and `island-init/data/lis-geometry.json` (`island_init/render.py`'s `TARGETS`/`LIS_GEOMETRY_TARGET`; `write_all` now creates the destination directory if needed); `island_init/apply.py`'s `RESTART_MAP` and `stack/services/compose.yaml`'s portal mount updated to match. The two old tracked paths are gone; `island-init/lis-geometry.example.json` and `stack/services/config/portal/settings.example.json` are their replacements — static, tracked snapshots rendered once from `island.example.yaml`, touched only by hand when the example itself changes, never by `render`/`apply`. The golden-rule pytest (`tests/test_render.py`) now diffs those two targets against their dedicated `*.example.json` snapshot instead of the (now gitignored, instance-specific) live output; `pytest` 58 passed (unchanged count — same tests, adjusted comparison). `island.sh up` gained `render_instance_outputs` (calls `island-init render --file island-init/island.yaml`) before `stack/services` comes up, so the data/ outputs exist on a fresh clone the same way 2.1-f made `island.yaml`/`secrets/` exist. **Verified literally**: staged a draft with an extra site against a running instance's `island.yaml` (same shape the console's `/api/draft` produces) and ran `island.sh apply` on the host — signed, rendered, `island-init check` passed, and `git status` showed no change outside gitignored `data/` (caught one real gotcha doing this: `island.sh` shells out to the *pipx-installed* `island-init`, not the live repo source — a stale install re-ran the old code and dirtied the old tracked paths on the first attempt; `pipx install --force ./island-init` after any local `island_init` edit, now called out in island-init/README.md). Then, with all Docker state purged, re-ran the full fresh-clone path from scratch (`git clone` into an empty directory → `pipx install ./sim-tools ./island-init` → `./island.sh up`, 1m 58s → `stack/services/verify.sh`, 43s) — `ALL CHECKS PASSED`, `stack/services/data/portal/settings.json` and `island-init/data/lis-geometry.json` present as real files (not the directory-placeholder symptom) and correctly gitignored, `pytest` 58 passed from a second, fully independent venv built against that same clone, tree clean throughout. 2.1-e (blocked) remains open | 2026-09-14 |
| 2.2–2.4 | Class C / B / A reference designs | open | — | — |
| 2.5 | Power engineering (solar sizing, battery profiles) | open | — | — |
| 3.1 | Threat model | partial | `docs/threat-model.md` v0.1 published (assets, actors, trust boundaries, per-component mitigations, accepted disclosures); external review not yet done — that is the remaining acceptance | 2026-09-14 |
| 3.2 | Provisioning tooling | partial | `sim-tools/README.md` "Verification run" sections (3.2-a/b/e of four subtasks: 3.2-a per 2026-09-12 run; 3.2-b — `db sync` synced twice with no duplicates, removing a subscriber locally + sync produced a live UERANSIM auth failure (`FIVEG_SERVICES_NOT_ALLOWED`, AMF `Cannot find SUCI [404]`), `stack/core/verify.sh` re-passed with the new loader; 3.2-e — rig-safe sync semantics (`sub add --if-missing`, unchanged-skip `db sync`) fixed *two* real bugs found by actually running the multi-rig scenario, not just by inspection: the original shared-store-wipe bug (reproduced live first — OAI's IMSI `…0002` found deleted from Mongo while its UE's PDU session was still up) and a second, previously-undocumented one where Open5GS's own runtime writes (`imeisv`, `security.sqn`) made an unchanged subscriber look "changed" to a naive full-document diff, still triggering a session-breaking `replaceOne` — see "Verification run (3.2-e acceptance)". Full re-run clean: `stack/ran/verify-5g-oai.sh` end-to-end with the UERANSIM rig already attached — `ALL CHECKS PASSED`, `db.subscribers.countDocuments()` = 2, both PDU sessions survived; `db sync` run twice reports `unchanged=2` both times with the UE still pinging `10.46.0.1`; `grep -n "logs.*grep" stack/*/verify*.sh stack/ran/verify-*.sh` shows only corroboration lines in the four converted scripts (`stack/ran/verify-5g.sh` is a documented exception — no live-state analogue exists for its no-UE NGAP-only check); `pytest -q` 34 passed (9 new). 3.2-c (`[HW]` pySim programming) and 3.2-d (batch/field mode) remain open) | 2026-09-13 |
| 3.3 | Identity federation & roaming | partial | 3.3-a done: `island.yaml` gains `node.internal_base` (island_init/render.py `NODE_IP_OFFSETS`, mirroring the existing `SERVICE_IP_OFFSETS` pattern) so `core_net` and every NF's fixed address are a per-island render parameter instead of hardcoded `10.10.0.x` literals; `stack/core/compose.yaml` and every previously untemplated NF config (ausf/udm/udr/nssf/pcf/bsf/sgwc/sgwu.yaml) are now render targets. `--lab` still renders byte-identical (golden rule) — only `compose.yaml`'s `services_net` name and AMF's NGAP host port changed, to env-var-defaulted literals (`${SERVICES_NET_NAME:-resccom_services_net}`, `${AMF_NGAP_PORT:-38412}`) that preserve today's values by default. `island-init/island.example.b.yaml` is a second lab island fixture (distinct IMSI block, UE/services prefixes, DNS zone, node-internal base; same test PLMN per RFC-0003 D1; realm deliberately kept identical to island A — see the fixture's own comment on the freeDiameter TLS-cert constraint found live). `island.sh` gains `--island <dir>`. **Verified live**: a second local checkout, island B's fixture rendered into it, both islands' `stack/core` brought up SIMULTANEOUSLY on one host with distinct `core_net` bridges (10.10.0.0/24 / 10.20.0.0/24, confirmed via `docker network ls`) and no port/subnet collisions; each island's own `stack/core/verify.sh` ran while the other was fully up and reported `ALL CHECKS PASSED` (island A: UE 10.45.0.2, breakout 10.46.0.2, unchanged from before this task; island B: UE 10.47.0.2, breakout 10.48.0.2); `pytest` 58 passed. `stack/core/verify.sh` itself now derives its expected UE/services subnets from the rendered configs instead of hardcoding the lab's own values, since it's copied unchanged into a second island's tree. 3.3-b done: `island.yaml` gains `island.overlay.address` (this island's own WireGuard tunnel address, required for render v0) and `federation.peers[]` (RFC-0003 D2's allow-list — empty by default in both lab fixtures; an entry carries a peer's overlay pubkey, endpoint, `node_internal_base`, `services_prefix`). New render targets `stack/federation/compose.yaml` + `config/wg-up.sh` bring up one `wg-overlay` container per island (pinned `linuxserver/wireguard:1.0.20250521`, unmodified) that reads its private key straight from a single mounted `secrets/<island-id>/wireguard.key` via `wg set wg0 private-key <file>` — never written into any rendered file (2.1-f mount discipline: that one file only, not `secrets/` wholesale). A real constraint found live: `core_net`'s `internal: true` (1.1-c) drops any bridged packet whose source/destination isn't itself a `core_net` member, even between two containers on that same bridge — so `amf` (the only container that originates a cross-island SBI call) instead routes through `wg-overlay` via `services_net` (not `internal`), gaining `cap_add: NET_ADMIN` and that network membership only when a peer is allow-listed (golden rule holds: an unpeered island's `amf`/compose.yaml is byte-identical to before). **Verified live, all three acceptance checks, both directions**: (1) from A's `amf`, B's NRF+UDM SBI ports answered over the tunnel via real generated WireGuard keypairs, and the reverse — `stack/federation/verify.sh` `ALL CHECKS PASSED` both ways; (2) removing the peer from either side's allow-list, re-rendering, and restarting that side's `wg-overlay` made the same SBI check fail (`Connection timed out`), confirmed both directions; (3) stopping `wg-overlay` on either island left that island's `stack/core/verify.sh` passing unchanged (`ALL CHECKS PASSED`), confirmed both islands. `pytest` 60 passed; `render --lab --diff` "no changes" after restoring both fixtures' empty allow-lists. Full trace: `stack/federation/README.md` "Status". 3.3-c/d and 4.1 remain open. **3.3-b follow-up (review findings, 2026-09-14) done**: (1) `stack/federation/two-island.sh {up,down,verify}` replaces the hand-run cross-peering procedure — generates both islands' real signed identities (`island-init new --lab` / the new `new --file`, generalizing `--lab` to any island.yaml-shaped source), cross-peers transient copies (never the committed fixtures), brings up both islands' `stack/core`+`stack/federation`, attaches island A's own UERANSIM UE only once its `amf` is in its final peered shape (peering recreates `amf` — cap_add + a services_net membership only render once a peer exists — so attaching first would just get dropped), runs `stack/federation/verify.sh` both directions, confirms island A's UE is *still* attached (the 3.2-e multi-rig scenario: federating a second island must not disrupt an already-attached local subscriber), tears down; **`./two-island.sh verify` verified live end to end (2026-09-15, re-run after a Docker Desktop update briefly broke `mongo:8.0.30` under its bundled kernel 7.0.12 — MongoDB's own known kernel 6.19–7.0.13 incompatibility, unrelated to this repo, resolved by downgrading Docker Desktop): `ALL CHECKS PASSED (both directions, island A's local UE undisturbed)`, `uesimtun0` held its `10.45.0.2` PDU session throughout B's bring-up and both directions' SBI checks — is what this and future 3.3-c/d/4.1 work should cite. (2) `wg-overlay` and a peered `amf` now get a fixed `services_net` address (`WG_OVERLAY_SERVICES_OFFSET`/`AMF_SERVICES_OFFSET`) instead of a dynamic membership; `stack/backhaul/unplug-test.sh`'s WAN guard rewritten to track every network membership (not just fixed-IP ones) and fail loudly on a dynamic one — extending it surfaced two real pre-existing gaps (console's address never added to `WAN_TOUCHING_IPS`; `matrix_net`/`meet.jitsi` never marked `internal: true`, giving Docker's default NAT a latent WAN route), fixed in the same commit and verified live (`stack/services/verify.sh` `ALL CHECKS PASSED` after recreating both networks — real E2EE Matrix message, real bidirectional Jitsi audio). (4) `island-init check`/`render` and `wg-up.sh` itself now catch a declared `overlay.wireguard_public_key` drifting from the real key file (found live after 3.3-b's own testing). (5) `island-init migrate` brings a stale instance current with schema changes (3.3-a's `node`, 3.3-b's `federation.peers`) and re-signs it; `island.sh up` now refuses an instance that fails `check`. (6) an unpeered `stack/federation/verify.sh` prints a message and exits 0 instead of failing (a `pipefail`/`set -e` bug in the fix's first pass made this not actually happen until caught live). Also fixed: `wg-up.sh`'s content depends on the (freshly-random-per-`new`) declared public key, so it moved to gitignored `stack/federation/data/` (2.1-g's own pattern) after `island.sh up` was found dirtying the tracked tree on a real instance's first render. `pytest` 77 passed; `render --lab --diff` "no changes". **(7) done (found in review, 2026-09-15)**: `verify` used to leave island A peered in the tracked tree (its `compose.yaml`/`wg-up.sh` never reverted) and `down` unconditionally `down -v`'d island A's core, destroying whatever subscriber data an already-running island A had. Fixed: a gitignored state file records whether island A's core pre-existed the harness run — `down` only `down -v`'s it if the harness itself created it — and `down` always re-renders island A's tracked `stack/` configs from its own real, unpeered `island-init/island.yaml` afterward, asserting `git status --porcelain -- stack/` is empty and failing loudly otherwise. **Verified live**: with island A pre-existing and holding three subscribers (including a marker IMSI added for this test), `two-island.sh verify` `ALL CHECKS PASSED` both directions, island A's UE stayed attached; immediately after, `git status --porcelain` showed no diff beyond the fix's own files, a standalone `stack/federation/verify.sh` printed "no peers in island.yaml -- nothing to verify" and exited 0, and island A's mongo volume still held all three pre-run subscribers — confirmed across a mid-test Docker Desktop restart (needed to clear an unrelated stuck host-level hairpin-NAT state, not a code issue). **(8) done (found live, 2026-09-17)**: item 7's state file was trusted from disk, so a `roam` run that died before `down`'s `rm -f` left a stale `a_core_preexisting=0` that the next run's `down -v` acted on, destroying island A's real dev core (recovered via `island.sh up`; subscribers live in the sim-tools store). `up` now re-detects island A's live state first thing every run (no container *and* no compose volume = created from nothing) and overwrites the file; `down` destroys A only on an explicit `=0` and treats a missing file as "leave A up". Verified live: with A up and a planted stale `=0`, `two-island.sh verify` rewrote it to `=1`, `ALL CHECKS PASSED` both directions, A's 16 core containers and `core_mongo-data` survived; a standalone `down` with no state file also left A up. **3.3-c: negative finding (2026-09-15)**, not done. `stack/federation/two-island.sh roam` + `roam_test.py` is the reproducible vehicle: registers island A's real AUSF/UDM into island B's NRF with `ausfInfo`/`udmInfo.supiRanges` covering A's IMSI block, provisions a roaming-test subscriber on A only, attaches it via B's own gNB, and prints every acceptance bullet's PASS/FAIL plus B's full AMF log. Settled live, reproducibly, against the pinned `gradiant/open5gs:2.8.0`: NRF silently drops `supiRanges` on persist (a raw PUT/GET round-trip proves it); a one-shot "static profile" doesn't survive NRF's own heartbeat-timeout (NF_DEREGISTERED within ~10s, nothing kept it alive); AMF does not re-select an AUSF/UDM instance per subscriber identity even when a second, correctly-shaped candidate is present (reused its already-cached local AUSF regardless). The roaming UE's registration reliably rejects with `Registration reject [7]` (5GS services not allowed) — B's own AUSF correctly reports "Cannot find SUCI" for a subscriber it has never heard of. B's Mongo holds zero documents for the roaming IMSI throughout, for the uninteresting reason that authentication never got that far. Open5GS cannot be patched here (CLAUDE.md); SEPP/N32 is RFC-0003's own named alternative but needs a design pass this task didn't do and doesn't fit the lab's same-PLMN model as-is. Full evidence and conclusion: `stack/federation/TASKS.md` 3.3-c, `rfcs/rfc-0003-roaming.md` open question 1. **3.3-b follow-up (3) done**: independent of whether 3.3-c's roaming ever works, `wg-overlay`'s forward-filter now restricts tunnel-origin traffic to this island's own NRF/AUSF/UDM on their SBI port (iptables FORWARD ACCEPT for those three, DROP for the rest of `core_net`) — closes threat-model M8's "an allow-listed peer can reach every NF" gap. Verified live: `two-island.sh verify` still `ALL CHECKS PASSED (both directions, island A's local UE undisturbed)` with the filter active; `render --lab --diff` required regenerating `stack/federation/config/wg-up.example.sh`; `pytest` 77 passed. **3.3-c v2 done (partial): SEPP/N32 distinct-PLMN roaming (RFC-0003 D1 amendment, 2026-09-15)**, supersedes 3.3-c's closed same-PLMN mechanism. `island-init` now renders the real upstream SEPP/N32 topology (`configs/examples/5gc-no-scp-sepp*.yaml.in`, read first, not invented) from `island.yaml`/`federation.peers[]`: island B's lab fixture moves to PLMN 999/70 (mirroring upstream's own pairing), a new `sepp` service (TLS identity picked by PLMN from the image's own bundled sepp1/sepp2 certs, N32-c/N32-f on one core_net address via two ports — not upstream's three separate addresses, which don't fit this stack's one-address-per-NF convention), `amf.yaml` gains a peer-conditional `access_control` list, and the overlay forward-filter narrows from NRF/AUSF/UDM to SEPP's N32 ports only. `two-island.sh roam` is the reproducible vehicle (rewritten for the new topology; the old SUPI-range mechanism stays reproducible as `roam-legacy`). **Verified live, two distinct results**: (1) SEPP's own N32-c security association genuinely establishes between the two distinct-PLMN islands over the real WireGuard overlay (`SEPP established` in both sides' own logs) — the core mechanism works, not just renders. (2) The UE itself cannot be attached end-to-end in this `[SIM]` lab: it correctly finds island B's cell as PLMN 999/70 but only as `category[ACCEPTABLE]`, and UERANSIM v3.3.0's own PLMN-selection source (`src/ue/rrc/idle.cpp` `lookForSuitableCell`, `src/ue/nas/mm/base.cpp`) never attempts registration from an ACCEPTABLE (non-home-PLMN) cell — a UERANSIM limitation, not a defect in this repo's rendering or in SEPP/N32 itself; no config or `nr-cli` override was found. `pytest` 77 passed; `render --lab --diff` clean. Full evidence: `stack/federation/TASKS.md` 3.3-c v2, `rfcs/rfc-0003-roaming.md`. `[HW]` RAN work (a real phone does standard PLMN selection) is the likely path to close the remaining gap. **3.3-c v2b done (partial): closing the UE-attach gap with OAI nrUE instead of waiting on `[HW]` (2026-09-15).** `two-island.sh roam --ue oai` adds a second, `[SIM]`-legal UE path: OpenAirInterface's nrUE (`stack/ran`'s existing 1.2-a(5G-alt) rig), whose upstream `openair2/RRC/NR_UE/rrc_UE.c` has no PLMN-suitability check, unlike UERANSIM. **Verified live**: the VERIFY marker's both halves confirmed — the nrUE's Registration Request on island B's 999/70 cell does carry a `suci-0-001-01-...` SUCI (B's AMF logs it directly), and B's AMF does attempt SEPP-based home-routed discovery (`Try to discover [nausf-auth]` → NRF's own inter-PLMN path → `NRF Found [fqdn:nrf.5gc.mnc001.mcc001.3gppnetwork.org...]`), not a PLMN-mismatch rejection. Two real, config-only lab-profile gaps found and fixed en route (island A's NRF gains a peer-conditional port-80 listener, since Open5GS's own inter-PLMN NRF-discovery client never sets a port when there's no `hnrf-uri`; island A's SEPP gains an `extra_hosts` entry so it can resolve its own synthetic 3GPP FQDN once a forwarded request "arrives home," standing in for the DNS this lab doesn't run) get the request from unreachable (500/connection-refused) to a real `200 OK` from A's own NRF. **Still doesn't complete**: the response carries no AUSF candidate — B's AMF's own next line is `(NF discover) No [nausf-auth]`, then `Registration reject [90]`; root cause not isolated further (hypothesis: SEPP's URI-reconstruction across the N32 hop may drop/mismatch the discovery query string — untested). A live re-run through the finished, committed `two-island.sh roam --ue oai` to reconfirm both fixes end-to-end was blocked four separate times by an unrelated Docker Desktop host-networking fault (asymmetric one-directional WireGuard UDP NAT, `wg show` transfer counters stuck at 0 in one direction) — not a code issue: the fourth attempt, through the actual script, still failed at `up`'s own SEPP N32 overlay-reachability check, before any OAI-specific code ran at all, confirming the fault isn't OAI-path-specific and isn't cleared by going through the finished tool. Not chased further this task since fixing it needs a full Docker Desktop restart disruptive to island A's other long-running services; the finding itself rests on hand-edited-then-templated live evidence, confirmed byte-identical by `pytest`. Roaming-test subscriber leftover on A (open since the 3.3-c v2 review) fixed in the same commit: `two-island.sh down` now removes it from both A's local store and Mongo — **confirmed live via the actual script**, in that same fourth attempt's own teardown (`removed 001010000099999`). `pytest` 77 passed; `render --lab --diff` clean. Full evidence: `stack/federation/TASKS.md` 3.3-c v2b, `rfcs/rfc-0003-roaming.md`. Two independent UE implementations now agree in outcome (blocked, for two different, well-characterized reasons); `[HW]` phone roaming is the only path left untried, and the empty-discovery-response gap is a concrete, narrow next step short of it. **3.3-c v2c done (partial): three real script bugs found and fixed; its stall finding is superseded by the review's re-run, v2d is the next step (2026-09-16).** A genuine Docker Desktop restart (the first attempt silently no-op'd — the daemon stayed in a persistent 500 error state with the backend's own process start time unchanged; a real force-kill + relaunch was needed) plus cleanup of two stale orphan containers (a leftover UERANSIM pair squatting mongo's IP, a leftover peered `sepp` crash-looping against the now-unpeered config) got `two-island.sh verify` passing clean again, satisfying this task's own precondition. A new `node.log_level` knob (island-init, default `info`, golden rule intact) makes island A's NFs debuggable at `debug` level. Running that through the real, finished `two-island.sh roam --ue oai` (not hand-edited config, unlike every prior confirmation of the "200 OK" finding) surfaced three genuine script/orchestration bugs, fixed in the same commit: (1) `nrf`'s own compose service definition never changes on peering, so an already-running, previously-unpeered island A core (the normal dev-core case) never actually reloads the peer-conditional port-80 fix from 3.3-c v2b without an explicit restart the harness wasn't doing — every prior "200 OK" was via hand-edited config *restarted by hand*, sidestepping this exact gap; (2) OAI gNB's NG-Setup poll budget (30s) is sometimes too short under real load (observed 60s once; isolated manual runs <2s) — extended to 120s, with the gNB's own log now dumped on failure; (3) the acceptance check's own "AUSF/UDM logged an authentication request" line was a false positive, matching island A's own already-attached local UE's routine re-auth (same home-PLMN SUCI prefix) instead of the roaming subscriber's — scoped to the roaming subscriber's own MSIN. **With all three fixed, a fresh run through the real tool gives a more precise finding than the task anticipated**: A's SEPP does reach A's NRF now (TCP ESTABLISHED on both ends, confirmed live via each container's own `/proc/net/tcp`) and Open5GS's own SBI client reports successful dispatch — but no HTTP/2 exchange ever completes on that connection, checked again unchanged 12+ minutes later, while the same NRF process demonstrably keeps servicing other NFs throughout. This means the request never reaches NRF's own discovery-handling code (`nnrf-handler.c`) at all — so this negative finding does **not** name an excluding line there, contrary to what the task's own precondition text anticipated from the prior review's source-only analysis; the actual stall is one hop earlier, in SEPP's own request dispatch to a second, non-primary `sbi.server` listener, and isolating it further needs `gdb`/`strace` against the running process, out of scope here. Not fixable in this repo (CLAUDE.md "never fork upstreams") — an upstream Open5GS issue is the concrete next step. `pytest` 77 passed; `render --lab --diff` clean; golden rule confirmed intact for the new knob. Full evidence: `stack/federation/TASKS.md` 3.3-c v2c, `rfcs/rfc-0003-roaming.md`, `docs/threat-model.md` M8. **M3 remains open, but the block is in this project's rendering, not in Open5GS**: the review's re-run of the v2c tool (recorded at the end of `stack/federation/TASKS.md` 3.3-c v2c) showed the home-routed discovery completing in 23 ms and returning A's AUSF; the AUSF request then bypasses SEPP because our NF profiles carry IP addresses, not 3GPP FQDNs (`lib/sbi/path.c:805` routes via SEPP only for a foreign FQDN), and A's overlay forward filter correctly blocked it. The v2c stall did not reproduce on a healthy Docker host, so no upstream report is filed. **3.3-c v2d done (partial): the UE-attach gap is closed — first full cross-island authentication and registration success in this project (2026-09-16).** Rendered what upstream's own home-PLMN roaming example actually renders: NRF/AUSF/UDM/SMF/NSSF addressed by 3GPP FQDN (`<nf>.5gc.mnc<mnc>.mcc<mcc>.3gppnetwork.org`) as their primary `sbi.server` listener (AMF/PCF/BSF/UDR/SEPP keep plain `core_net` IP:7777, matching upstream exactly), `extra_hosts` on every SBI container resolving every FQDN (no DNS server in this lab), `serving:` PLMN added to `amf.yaml`/`smf.yaml` so Open5GS's own foreign-PLMN check has a home PLMN to compare against. This deliberately changes the lab profile — golden rule re-baselined in the same commit (`pytest` 77 green). A second, unanticipated gap surfaced live once a *second* FQDN-addressed NF (not just NRF) was actually exercised cross-island and fixed the same way: AUSF/UDM/SMF/NSSF needed upstream's real default port 80, not this task's own initial "keep 7777" simplification — SEPP's "arrived home" forwarding rebuilds the target URL from the bare FQDN with no port, landing on the SBI client's own default regardless of what the NF actually listens on. **With both fixes, verified live, exactly the signatures asked for**: A's NRF logs `NF-Discovered […,FQDN:ausf.5gc.mnc001.mcc001.3gppnetwork.org]` (no longer `FQDN:NULL`); B's AMF logs `Setup NF EndPoint(fqdn) [ausf.5gc.mnc001.mcc001.3gppnetwork.org:7777]` with no `Couldn't connect`; A's AUSF logs genuine `AUTHENTICATION_SUCCESS` for the roaming subscriber with real derived keys; the roaming nrUE receives a real `Registration Accept` and the harness's own live-state gate confirms `UE registered on B: YES`. K never left island A. Settled the recorded oddity precisely from the real discovery query string: `requester-plmn-list` is present, `target-plmn-list` never appears, in any run. `stack/core/verify.sh`, `stack/services/verify.sh` (checks 1–5; checks 6/7 hit unrelated external-network flakiness reaching pypi.org/npmjs.org, not this repo, both later confirmed passing on retry), `stack/ran/verify-5g-oai.sh`, and `two-island.sh verify` all pass clean on the re-baselined addressing — nothing but addressing changed single-island. **A genuinely new finding, immediately after**: what's actually happening is real 3GPP Home-Routed (HR) roaming, not the Local Breakout (LBO) RFC-0003 D1 specifies — Open5GS's AMF unconditionally selects HR the moment `serving:` makes the foreign-PLMN check true, and the alternative is a per-subscriber document flag, see the v2d review note. Confirmed live: A's own SMF/UPF, not B's, anchor the roaming subscriber's PDU session and allocate its IP from A's own pool — real, standards-compliant HR roaming, not a stub — but it needs an N9 (UPF↔UPF GTP-U) tunnel across the overlay that the forward filter (by design, 3.3-b follow-up 3/3.3-c v2) does not carry, so the breakout ping still fails — now because the filter is *correctly* blocking a hop this design never intended to need, not an addressing defect. Also fixed in the same commit: `two-island.sh`'s OAI roaming branch was missing the same `ip route add <subnet> dev oaitun_ue1` step `stack/ran/verify-5g-oai.sh` already carries for its own single-island check (OAI's nrUE never installs a route beyond its own PDU-session subnet) — necessary but not sufficient once the real HR-roaming/N9 gap was found underneath it. Full evidence: `stack/federation/TASKS.md` 3.3-c v2d, `rfcs/rfc-0003-roaming.md` (Follow-up 4, open question 5), `docs/threat-model.md` M8, `stack/core/README.md` addressing section. **Next task: 3.3-c v2e** — decide Local-Breakout-vs-Home-Routed roaming and, if HR is accepted as this project's actual mode, give N9 crossing the overlay the same explicit RFC-0003-level design pass N32 already got.**Review of v2d (2026-09-16)**: reproduced through the committed tool (Registration Accept, UE registered on B, A's SEPP forwarded AUSF and UDM requests, restore clean), `stack/core/verify.sh` and `verify-5g-oai.sh` green on the new addressing, `pytest` 77 green from any cwd. Correction: `lbo_roaming_allowed` is a per-subscriber field of the Open5GS subscriber document (upstream roaming tutorial §2), not dead code — Local Breakout as RFC-0003 D1 specifies is a `resccom-sim` change, **3.3-c v2e**, and M3 needs no N9 tunnel. M3 status after v2d: control plane proven, data plane pending v2e. **3.3-c v2e done in code, not verified live — M3 is not reached in software as of this commit (2026-09-16).** `resccom-sim` now writes `lbo_roaming_allowed` (default `true`, RFC-0003 D1's Local Breakout) into every subscriber's `slice[].session[]` document; `sub add`/`sub edit --home-routed` opt a subscriber into Home-Routed instead, `sub show`/`sub list` display the mode, and a pre-existing store migrates in place (existing rows default to `true`). Applied live to island A's real dev store with no UE holding an active session (the documented `db sync` quirk): `added=0 updated=3 unchanged=0 removed=0`, confirmed via a direct Mongo query showing `lbo_roaming_allowed: true` on all three existing subscribers; `stack/core/verify.sh` re-passed clean (`ALL CHECKS PASSED`) on the changed documents. `two-island.sh roam --ue oai`'s evidence section was rewritten per this task: the log-level-dependent AUSF/UDM grep (the v2d review's own finding) is replaced by A's SEPP's info-level forward lines plus B's AUSF/UDM logging nothing for the roaming MSIN, and new checks were added for B's SMF anchoring the session with an address in B's UE pool and no `HR Roaming in V-SMF`, A's SMF/UPF logging nothing for the IMSI, and the breakout ping's exact `0% packet loss`. **Could not be exercised live this session**: `two-island.sh verify`'s own precondition failed reproducibly (`FAIL: sepp could not reach 10.20.0.16:7778 over the overlay`) on four separate attempts — two before and two after a genuine Docker Desktop force-restart, confirmed via the VM's own `uptime` (10 minutes, not just the app relaunching) — which rules out ordinary staleness as the explanation the v2c precondition note offered. Diagnosed one step further than any prior write-up in this family: `wg show`'s own transfer counters, read live mid-run, show A's *outbound* path to B is the one black-holed (A: `2.36 KiB sent, 2.17 KiB received`; B: `2.17 KiB sent, 0 B received`, both counters growing over a real multi-minute SEPP TCP-connect attempt, never converging), and A's own recorded peer-endpoint for B is a `services_net`-local, NAT-translated address (`10.46.0.1:<ephemeral>`), not a route to B at all — consistent with WireGuard's own endpoint-roaming (it re-learns a peer's endpoint from the source address of the last packet received from it) being poisoned by Docker Desktop's NAT hairpin when two containers on the same host reach each other only through each other's host-published overlay ports. This is a host-networking limitation of running both islands as containers on one Docker-Desktop-for-Mac host, not a defect in this task's own code (`sim-tools/resccom_sim/{cli,model,store,sync}.py`, `two-island.sh`'s evidence section — none of which touch `wg-up.sh` or overlay routing). A candidate fix (periodically re-asserting each peer's configured endpoint against WireGuard's own re-learning) is noted as a follow-up, not attempted here — out of this task's scope. **Net M3 status: control plane proven (v2d, unaffected by this finding), Local Breakout implemented and verified at the unit/single-island level (v2e), but the roaming data-plane acceptance itself is unproven live in software this session** — do not read this row as M3 reached. Full evidence: `stack/federation/TASKS.md` 3.3-c v2e, `rfcs/rfc-0003-roaming.md` D1, `docs/threat-model.md` M8. **3.3-c v2f done: the overlay's own NAT-hairpin block (v2e) is fixed and verified live, twice — M3 is still not reached, one hop further (2026-09-16).** `stack/federation/compose.wan.yaml` (hand-written, not `island-init`-rendered) gives both islands' `wg-overlay` containers a dedicated `wan_net` bridge (`10.200.0.0/24`, static addresses) instead of reaching each other through Docker Desktop's host-published ports; `two_island_setup.py` renders each peer's endpoint as that static address; `two-island.sh` owns the network's lifecycle; `verify.sh` includes it in its own `up -d` only when the harness set `WG_WAN_IP`, so a standalone `./verify.sh` is unaffected. **Verified live**: `two-island.sh verify` `ALL CHECKS PASSED` both directions (SEPP reached the peer's N32-c/N32-f each way), with no recurrence of v2e's bilateral endpoint-drift symptom on either of two subsequent `roam --ue oai` runs. Both `roam --ue oai` runs (reproduced identically): the roaming nrUE gets a real `Registration Accept`, B's AMF logs `Registration complete`, A's SEPP forwards both the AUSF and UDM requests home, and B's AMF correctly selects Local Breakout — `LBO[1]`, `V-SMF discovered in Non-Roaming or LBO-Roaming[1]`, B's own local SMF endpoint set up — v2d's control-plane result and v2e's LBO flag both confirmed live through the actual finished tool for the first time. **New blocker, found both runs**: 13–40ms after AMF contacts B's own local SMF, `[1:0] PDU session establishment reject (../src/amf/namf-handler.c:486)`, with no connection-failure warning near it (a real, fast application-level reject, not a network fault) — B's own SMF's stated reason isn't captured by the harness's evidence dump and the container is gone before it can be inspected further. B's Mongo held zero documents for the roaming IMSI both runs; A's SMF/UPF logged nothing for it; both runs' teardown ended tree-clean. **M3 is not reached in software as of this commit** — the overlay is no longer the blocker, but PDU Session Establishment against B's own local SMF now is. Full evidence: `stack/federation/TASKS.md` 3.3-c v2f, `rfcs/rfc-0003-roaming.md` D1, `docs/threat-model.md` M8. **Review of v2g (2026-09-17)**: the PCF gap is closed by data, not upstream — policy-only inbound-roamer records (3.3-c v2h) — so M3 is not a negative finding; next task v2h. 3.3-d and 4.1 remain open. **3.3-c v2g done: M3 closed as a settled negative finding at the PDU-session layer (2026-09-17).** A `--log-level` knob on `two-island.sh` (applied to both islands' transient peered `island.yaml` copies only) plus the v2f follow-up's SMF-log dump got the exact cause on the first live run: B's own local PCF answers SMF's SM Policy Association request with a real `404 {"title":"[imsi-001010000099999:1] Cannot find SUPI in DB"}` (`../src/smf/gsm-sm.c:728`), because `pcf.yaml.j2` renders `db_uri: mongodb://mongo/open5gs` — PCF reads subscriber policy data by a direct local MongoDB connection, never an SBI/NRF-discovered call, so there is no hop for SEPP or any rendering fix to route through; the roaming subscriber's document exists only in island A's Mongo, by RFC-0003 D1's own design (subscriber identity stays home). This is Open5GS's own architecture (no N24 V-PCF↔H-PCF roaming interface), not a defect in this project's rendering — CLAUDE.md "never fork upstreams" applies, so this is documented as a negative finding, not patched. **Everything up to this point is real and reproducible**: authentication (v2d), Local Breakout selection (this run, B's AMF logs `LBO[1]` and its own local SMF endpoint), the overlay (v2f) — RFC-0003 D1's home-routed-authentication clause is fully settled live. Its "sessions and services stay local" clause cannot complete against Open5GS's shipped PCF for a subscriber whose identity correctly stays home. Full evidence and exact log lines: `stack/federation/TASKS.md` 3.3-c v2g, `rfcs/rfc-0003-roaming.md` D1, `docs/threat-model.md` M8. **3.3-c v2h done: MILESTONE M3 IS REACHED IN SOFTWARE (2026-09-17).** v2g's review corrected the conclusion from source: Open5GS's PCF needs only a subscriber's `slice[].session[]` policy for SM Policy Association (`lib/dbi/session.c`), never `security` -- so the fix is data, not a workaround. `sim-tools` gained `resccom-sim roaming export`/`import`/`list`/`remove`: the home island exports a roaming-enabled subscriber's policy only (no key, no opc, no security field anywhere -- refused on import, enforced in code), the visited island imports it marked `resccom_inbound_roamer: true`, and `db sync` now excludes and skips marked records so a local sync never disturbs them. `two-island.sh roam` exports/imports before the UE attaches. **Run twice live (`./two-island.sh --log-level debug roam --ue oai`), both runs identical, every acceptance bullet passed on the first attempt each time**: real `Registration Accept`, UE address `10.47.0.2` in B's own UE pool (Local Breakout, not Home-Routed), A's SEPP forwarded both AUSF and UDM requests home, B's AUSF/UDM logged nothing for the roaming MSIN (K never left A), B's Mongo held exactly one marked, security-free policy record (`count=1 marked=yes no_security=yes`), B's own SMF/UPF anchored the session with no `HR Roaming in V-SMF`, A's SMF/UPF logged nothing, and the breakout ping to B's own services subnet completed **`0% packet loss`** both runs. Both teardowns restored island A's tree clean and left its real dev core (19 containers) untouched -- 3.3-b follow-up 8's fix held under real concurrent use. `stack/core/verify.sh` and `pytest` (82 sim-tools, 77 island-init, golden rule intact) all green. **What's proven**: RFC-0003 D1's full text (home-routed authentication AND sessions/services stay local) for 5G, `[SIM]` path -- two distinct-PLMN islands, real SEPP/N32, K never leaving home, a roaming subscriber genuinely served by the visited island's own SMF/UPF. **What isn't**: `[HW]` roaming (a real phone, separate real hosts) and 4G inter-realm roaming (3.3-d) remain open; this milestone is 5G-only. Privacy cost now recorded in RFC-0003 D1/D5: the visited island learns roamer IMSIs at provisioning time (when their policy is exported), not only at first attach. Full evidence, exact log lines, both runs: `stack/federation/TASKS.md` 3.3-c v2h, `rfcs/rfc-0003-roaming.md` D1/D5, `docs/threat-model.md` M7/M8, `docs/poc-writeup.md`, `sim-tools/README.md` "Inbound roaming". | 2026-09-17 |
| 3.4 | PKI & secure updates | open | — | — |
| 4.1 | Two-island exercise (roaming partition drill) | done-verified | `stack/federation/two-island.sh drill --ue oai` 5/5 steps AS EXPECTED (live session survives the cut, fresh registration fails, guest provisioning works, recovery 7s); `stack/federation/TASKS.md` 4.1, `docs/poc-writeup.md`, `playbook/drills.md`; Matrix half (4.1-b) deliberately out of scope | 2026-09-17 |
| 4.2 | Delay-tolerant sync | open | — | — |
| 4.3 | Island emergency service + upstream routes (RFC-0004) | open | — | — |
| 4.4 | IMS/VoLTE spike (go/no-go) | open | — | — |
| 5.1–5.4 | Pilot, hardening, v1.0 launch | open | — | — |
