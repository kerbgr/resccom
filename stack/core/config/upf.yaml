################################################################################
# ResCCOM 1.1-a — UPF (User Plane Function)
# Derived from upstream open5gs/open5gs configs/open5gs/upf.yaml.in (v2.8.0).
# UE subnet 10.45.0.0/16 (rendered from island.yaml by island-init
# render — do not hand-edit).
################################################################################
logger:
  file:
    path: /opt/open5gs/var/log/open5gs/upf.log

global:
  max:
    ue: 1024

upf:
  pfcp:
    server:
      - address: 10.10.0.7
  gtpu:
    server:
      - address: 10.10.0.7
  session:
    - subnet: 10.45.0.0/16
      gateway: 10.45.0.1
  metrics:
    server:
      - address: 10.10.0.7
        port: 9090
